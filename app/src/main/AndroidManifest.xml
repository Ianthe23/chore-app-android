<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Notification permission for Android 13+ (2p requirement - system services) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Location permissions (3p requirement - sensors/location) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChoreApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.ChoreApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.ChoreApp.NoActionBar" />

        <!-- Chore List Activity -->
        <activity
            android:name=".ChoreListActivity"
            android:exported="false"
            android:theme="@style/Theme.ChoreApp.NoActionBar" />

        <!-- Chore Detail Activity -->
        <activity
            android:name=".ChoreDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.ChoreApp.NoActionBar"
            android:parentActivityName=".ChoreListActivity" />

        <!-- Location Picker Activity (3p requirement - Location & Maps) -->
        <activity
            android:name=".LocationPickerActivity"
            android:exported="false"
            android:theme="@style/Theme.ChoreApp.NoActionBar" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>

</manifest>